# Changelog

## 1.1.0

- Rewritten content to reflect the current system: lists the real apps
  (File Explorer, Atol, App Store, Settings, Process Monitor) and desktop
  features, and drops the stale "coming soon" section.
- Removed emoji in favour of the SVG icon set (`getIcon`), matching the rest
  of the UI.
- Moved styling into a design-token stylesheet (`styles/welcome.css`) so the
  window follows the light/dark theme.
- Added quick-launch buttons (Open Settings / App Store / Files).
- Corrected the window title to "Welcome to WebDesk OS".

## 1.0.0

- Initial welcome screen.
