services:
  backend:
    build:
      context: .
      dockerfile: backend/mock-host-agent/Dockerfile
    ports:
      - "8090:8080"
    volumes:
      - ./frontend:/app/frontend:ro
    restart: unless-stopped
